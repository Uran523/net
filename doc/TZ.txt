1) Cпектрограммы (2D картиток) 
2) Метаданные (Попробуйте пока без координат, а точнее входы для них создайте но подавайте пока нули)
    а) Сдвиг по оси частот имеет значение
    б) Частотный диапазон должен быть в числе метаданных обязательно
         (это горизонтальная ось 2D картинки)
    в) данные о координатах приемного и передающего пунктов (которые нам обязательно нужны, 
    наряду с другими метаданными) здесь пока в очевидном виде не извлекаются. 
    Там проблема в том, что те IG2 которые я вам дал - со старым именованием трасс, 
    в них нет в явном виде координат. Они есть в более новых IG2 (их я вам чуть позже дам). 
    Вот из старых как раз извлечь координаты не очень тривиально. Я это позже допишу.
3) Еще надо предусмотреть входы для индексов солнечной и магнитной активности.
(Этих индексов нет в ионограммах, но их надо будет притянуть (по дате и времени) из открытых баз)
